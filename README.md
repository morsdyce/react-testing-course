# React.js - basic to advanced course

```
git clone https://github.com/morsdyce/react-testing-course/tree/jest-base
git checkout jest-base
npm install
```

## Installation
start the project by running

    npm install && npm start
or if you use yarn

    yarn && yarn start
